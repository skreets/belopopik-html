$(document).ready(function(){

// Tooltipster
  $('.tooltip').tooltipster({
     contentAsHTML: true,
     position: 'top'
  });

  $('.tooltip-right').tooltipster({
     contentAsHTML: true,
     position: 'right'
  });

  $('.tooltip-bottom').tooltipster({
     contentAsHTML: true,
     position: 'bottom'
  });

  $('.tooltip-left').tooltipster({
     contentAsHTML: true,
     position: 'left'
  });

  $('.tooltip-top_right').tooltipster({
     contentAsHTML: true,
     position: 'top-right'
  });

  $('.tooltip-top_left').tooltipster({
     contentAsHTML: true,
     position: 'top-left'
  });

  $('.tooltip-bottom_right').tooltipster({
     contentAsHTML: true,
     position: 'bottom-right'
  });

  $('.tooltip-bottom_left').tooltipster({
     contentAsHTML: true,
     position: 'bottom-left'
  });
});